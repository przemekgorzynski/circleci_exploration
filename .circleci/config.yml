
version: 2.1

jobs:
  say-hello:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: "Say hello"
          command: "ls -la"

workflows:
  first-wf:
    jobs:
      - say-hello

  second-wf:
    jobs:
      - say-hello
